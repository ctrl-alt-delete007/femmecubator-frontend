import React from "react";
import MeetupEvents from "./meetup_events";
const Container = () => {
  return (
    <React.Fragment>
      <MeetupEvents />
    </React.Fragment>
  );
};

export default Container;
